# build output — regenerated by scripts/build_site.js on every deploy
site/

# the retired prototype (64 MB). Delete this line if you want it archived in git.
Quran_App/

node_modules/
.DS_Store
Thumbs.db
